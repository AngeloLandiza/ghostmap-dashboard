/**
 * Route entry for `/parties/:id`. The implementation lives in `src/pages/parties/`; this file
 * stays so `App.tsx` keeps its lazy import path (and its chunk).
 */
export { PartyLivePage, default } from './parties/PartyLivePage'
