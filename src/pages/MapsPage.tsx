/**
 * Route entry for `/`. The library lives in `src/pages/maps/` alongside the map viewer;
 * this file only keeps the lazy import in `App.tsx` pointing at it.
 */
export { MapsPage, default } from './maps/MapsPage'
