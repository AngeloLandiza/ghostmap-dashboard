/**
 * Route entry for `/parties`. The implementation lives in `src/pages/parties/` with the rest of
 * the party UI; this file stays so `App.tsx` keeps its lazy import path (and its chunk).
 */
export { PartiesPage, default } from './parties/PartiesPage'
