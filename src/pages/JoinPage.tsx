/**
 * Route entry for `/join/:code`. The implementation lives in `src/pages/parties/`; this file
 * stays so `App.tsx` keeps its lazy import path (and its chunk).
 */
export { JoinPage, default } from './parties/JoinPage'
