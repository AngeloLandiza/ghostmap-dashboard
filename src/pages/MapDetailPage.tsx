/**
 * Route entry for `/maps/:id`. The viewer lives in `src/pages/maps/` (and
 * `src/components/viewer/`); this file only keeps the lazy import in `App.tsx` pointing at it.
 */
export { MapDetailPage, default } from './maps/MapDetailPage'
